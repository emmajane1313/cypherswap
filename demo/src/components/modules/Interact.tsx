import { FunctionComponent } from "react";

const Interact: FunctionComponent = ({
  collectGrant,
  likeGrant,
  mirrorGrant,
}): JSX.Element => {
  return (
    <div className="relative flex items-center justify-center w-full h-10">
        
    </div>
  );
};

export default Interact;
